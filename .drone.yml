kind: pipeline
name: default

clone:
  disable: true

steps:
- name: clone
  image: docker:git
  commands:
  - git clone https://github.com/saptaka/privy_exam.git

- name: build
  image: golang
  commands:
  - go build
  - go test
  

trigger:
  branch:
  - master
  event:
  - push